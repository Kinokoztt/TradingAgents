# TradingAgents environment.
# Authoritative dependency list for reproducing the environment on any
# machine. Mirrors pyproject.toml [project].dependencies plus the optional
# "regime" extra (regime gate + concept graph) and dev/test tooling.
#
# Recommended setup (avoids the NumPy 1.x/2.x ABI breakage seen in some
# base conda envs):
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
# or, with uv:
#   uv pip install -r requirements.txt

# --- Core framework ---
langchain-core>=0.3.81
langchain-anthropic>=0.3.15
langchain-experimental>=0.3.4
langchain-google-genai>=4.0.0
langchain-openai>=0.3.23
langgraph>=0.4.8
langgraph-checkpoint-sqlite>=2.0.0
backtrader>=1.9.78.123
pandas>=2.3.0
parsel>=1.10.0
pytz>=2025.2
questionary>=2.1.0
redis>=6.2.0
requests>=2.32.4
rich>=14.0.0
typer>=0.21.0
setuptools>=80.9.0
stockstats>=0.6.5
tqdm>=4.67.1
typing-extensions>=4.14.0
yfinance>=0.2.63

# --- Regime gate + concept graph subsystem (optional "regime" extra) ---
# BigQuery access (macro_daily, day_aggs_di, candidate universe),
# Secret Manager (API keys), and the graph engine.
google-cloud-bigquery>=3.25.0
google-cloud-secret-manager>=2.20.0
networkx>=3.3
leidenalg>=0.10.2
python-igraph>=0.11.3

# --- Dev / test ---
pytest>=8.0
